{
	"name": "video-edit",
	"version": "0.1.0",
	"private": true,
	"description": "Root scripts for the video_edit Electron app.",
	"scripts": {
		"build": "pnpm --dir app run build",
		"start": "pnpm --dir app run start",
		"typecheck": "pnpm --dir app run typecheck",
		"lint": "pnpm --dir app run lint",
		"format": "pnpm --dir app run format",
		"check": "pnpm --dir app run check",
		"dist": "pnpm --dir app run dist",
		"dist:portable": "pnpm --dir app run dist:portable",
		"dist:installer": "pnpm --dir app run dist:installer"
	}
}
